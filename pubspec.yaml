name: habits_timer
description: A simple multi-platform habits timer built with Flutter + Riverpod.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  shared_preferences: ^2.5.3
  flutter_localizations:
    sdk: flutter
  fl_chart: ^0.68.0
  intl: 0.20.2
  isar: ^4.0.0
  isar_flutter_libs: ^4.0.0
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_riverpod: ^2.5.1
  build_runner: ^2.4.12
  isar_generator: ^4.0.0

flutter:
  uses-material-design: true

